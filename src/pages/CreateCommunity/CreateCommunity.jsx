export const CreateCommunity = ({}) => {
    return (
        <section className={'pt-12 flex items-center flex-col flex-wrap w-full'}>
            <h1>Creating a community</h1>

        </section>
    );
}
